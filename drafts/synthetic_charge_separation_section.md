# Synthetic Charge Separation Draft Section

This synthetic draft is for local testing only and contains no real paper claims.

## Charge Transfer Benchmark

The treated synthetic ZIS benchmark demonstrates stronger charge transfer and charge separation than the flat control [@zisCharge2025].

The paragraph can also cite the same local evidence with LaTeX-style syntax: \citep{zisCharge2025}. The wording is intentionally close to the structured claim so the local keyword matcher can find it.

Charge separation is discussed alongside catalyst stability in the project profile [@zisCharge2025; @zisStability2024].
