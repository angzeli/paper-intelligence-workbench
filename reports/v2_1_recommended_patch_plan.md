# v2.1 Recommended Patch Plan

## Focus

Use the v2.0 dogfood scaffold on a private real photocatalysis review and fix
only the highest-friction onboarding problems.

## Recommended Scope

- Improve metadata-backed planning after real-use feedback.
- Add clearer manual registry-entry examples if users still hesitate at the first paper.
- Tighten empty-project report messaging where stable commands feel noisy.
- Add dogfood smoke checks to release scripts if the workflow becomes the default quickstart.
- Consider a reviewed registry-draft export from `plan-from-files`, but only if it remains explicit and non-destructive.

## Do Not Expand Yet

- Do not add automatic PDF metadata extraction as authoritative metadata.
- Do not scrape or download papers.
- Do not add LLM summarization.
- Do not auto-generate claims or final prose.
- Do not import private references into committed fixtures.
